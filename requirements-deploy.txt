# Deployment pin set for online-store. ADDITIVE — requirements.txt is untouched.
#
# This exists for exactly one reason: requirements.txt pins
#
#     logging==0.4.9.6
#
# which is a 2005, Python-2-era distribution that squats the name of the Python
# standard library's own `logging` module. Nothing in this codebase needs it
# (`import logging` resolves to the stdlib), it has no business in a Python 3.12
# image, and a Python-2 sdist is not something you want pip trying to build
# inside a production image. It is dropped here and nowhere else.
#
# Everything below is requirements.txt verbatim, minus that one line.

blinker==1.9.0
certifi==2025.1.31
charset-normalizer==3.4.1
click==8.1.8
Faker==35.2.0
Flask==3.1.0
Flask-Login==0.6.3
greenlet==3.1.1
gunicorn==23.0.0
idna==3.10
itsdangerous==2.2.0
Jinja2==3.1.5
MarkupSafe==3.0.2
packaging==24.2
python-dateutil==2.9.0.post0
python-dotenv==1.0.1
requests==2.32.3
six==1.17.0
SQLAlchemy==2.0.37
typing_extensions==4.12.2
urllib3==2.3.0
Werkzeug==3.1.3
flask_sqlalchemy
flask_wtf
